# Twitter Clone

Полнофункциональный клон Twitter, созданный с использованием современного стека технологий: Next.js, React, TypeScript, MongoDB и NextAuth.js.

## 🚀 Особенности

- ✅ **Аутентификация**: Регистрация и авторизация пользователей с помощью NextAuth.js
- ✅ **Посты (твиты)**: Создание, просмотр и удаление постов
- ✅ **Лайки**: Возможность лайкать посты
- ✅ **Комментарии**: Комментирование постов с вложенной структурой
- ✅ **Подписки**: Система подписок на других пользователей
- ✅ **Уведомления**: Уведомления о лайках и новых подписчиках
- ✅ **Профили пользователей**: Персональные страницы с аватарами и обложками
- ✅ **Редактирование профиля**: Обновление имени, био, изображений
- ✅ **Responsive Design**: Адаптивный дизайн для всех устройств

## 🛠 Технологии

### Frontend
- **Next.js 16** - React-фреймворк с поддержкой SSR
- **React 19** - Библиотека для создания пользовательских интерфейсов
- **TypeScript** - Типизированный JavaScript
- **Tailwind CSS v4** - Utility-first CSS фреймворк
- **SWR** - React Hooks для загрузки данных
- **Zustand** - Легковесное управление состоянием
- **React Icons** - Набор иконок
- **React Hot Toast** - Красивые уведомления
- **React Dropzone** - Drag & drop загрузка файлов
- **React Spinners** - Индикаторы загрузки

### Backend
- **Next.js API Routes** - Serverless API endpoints
- **NextAuth.js** - Аутентификация для Next.js
- **Prisma** - ORM для работы с базой данных
- **MongoDB** - NoSQL база данных
- **Bcrypt** - Хеширование паролей
- **Axios** - HTTP клиент

## 📦 Установка

### Предварительные требования

- Node.js 18+
- MongoDB (локальная установка или MongoDB Atlas)
- npm или yarn

### Шаги установки

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd twitter
```

2. Установите зависимости:
```bash
npm install
```

3. Создайте файл `.env` в корне проекта:
```env
DATABASE_URL="mongodb://localhost:27017/twitter"
# или для MongoDB Atlas:
# DATABASE_URL="mongodb+srv://username:password@cluster.mongodb.net/twitter"

NEXTAUTH_JWT_SECRET="your-jwt-secret-key"
NEXTAUTH_SECRET="your-nextauth-secret-key"
```

4. Сгенерируйте Prisma Client и примените схему к базе данных:
```bash
npx prisma generate
npx prisma db push
```

5. Запустите сервер разработки:
```bash
npm run dev
```

6. Откройте [http://localhost:3000](http://localhost:3000) в браузере

## 🗂 Структура проекта

```
twitter/
├── components/              # React компоненты
│   ├── layout/             # Компоненты макета (Sidebar, Header, etc.)
│   ├── modals/             # Модальные окна (Login, Register, Edit)
│   ├── posts/              # Компоненты постов и комментариев
│   ├── users/              # Компоненты пользователей
│   ├── Form.tsx            # Форма создания постов/комментариев
│   ├── Avatar.tsx          # Компонент аватара
│   ├── Button.tsx          # Переиспользуемая кнопка
│   └── ...
├── hooks/                  # Custom React hooks
│   ├── useCurrentUser.ts   # Получение текущего пользователя
│   ├── usePosts.ts         # Работа с постами
│   ├── useUsers.ts         # Работа с пользователями
│   ├── useNotifications.ts # Работа с уведомлениями
│   ├── useLoginModal.ts    # Управление модальным окном входа
│   └── ...
├── libs/                   # Утилиты
│   ├── prismadb.ts         # Singleton Prisma Client
│   ├── serverAuth.ts       # Серверная аутентификация
│   └── fetcher.ts          # Axios fetcher для SWR
├── pages/                  # Next.js страницы и API routes
│   ├── api/                # API endpoints
│   │   ├── auth/           # NextAuth endpoints
│   │   ├── posts/          # CRUD операции для постов
│   │   ├── users/          # Операции с пользователями
│   │   ├── comments.ts     # Создание комментариев
│   │   ├── follow.ts       # Подписка/отписка
│   │   ├── like.ts         # Лайки
│   │   ├── notifications/  # Получение уведомлений
│   │   └── ...
│   ├── posts/[postId].tsx  # Страница отдельного поста
│   ├── users/[userId].tsx  # Страница профиля пользователя
│   ├── notifications.tsx   # Страница уведомлений
│   ├── index.tsx           # Главная страница
│   └── _app.tsx            # Обертка приложения
├── prisma/
│   ├── schema.prisma       # Prisma схема БД
│   └── client/             # Сгенерированный Prisma Client
├── public/                 # Статические файлы
├── styles/                 # Глобальные стили
└── CLAUDE.md              # Инструкции для AI ассистента

```

## 📊 Модели данных

### User
- Профильная информация (имя, username, био, email)
- Изображения (аватар, обложка)
- Хешированный пароль
- Массив ID подписок
- Флаг непрочитанных уведомлений

### Post
- Текст поста
- Автор (связь с User)
- Массив ID пользователей, поставивших лайк
- Комментарии (связь с Comment)

### Comment
- Текст комментария
- Автор (связь с User)
- Пост (связь с Post)

### Notification
- Текст уведомления
- Получатель (связь с User)

## 🔐 Аутентификация

Приложение использует **NextAuth.js** с провайдером Credentials:
- Пароли хешируются с помощью bcrypt
- Сессии основаны на JWT токенах
- Защищенные API routes проверяют аутентификацию через `serverAuth`
- Клиентские компоненты используют хук `useCurrentUser`

## 🎨 Стилизация

- **Tailwind CSS v4** с темной темой по умолчанию
- Адаптивная сетка с поддержкой мобильных устройств
- Кастомная цветовая схема (черный фон, небесно-голубые акценты)
- Utility-first подход для быстрой разработки

## 📝 API Endpoints

### Аутентификация
- `POST /api/register` - Регистрация нового пользователя
- `POST /api/auth/signin` - Вход в систему
- `POST /api/auth/signout` - Выход из системы
- `GET /api/current` - Получить текущего пользователя

### Посты
- `GET /api/posts` - Получить все посты
- `POST /api/posts` - Создать новый пост
- `GET /api/posts/[postId]` - Получить конкретный пост

### Пользователи
- `GET /api/users` - Получить всех пользователей
- `GET /api/users/[userId]` - Получить конкретного пользователя
- `PATCH /api/edit` - Обновить профиль

### Взаимодействия
- `POST /api/like?postId=...` - Поставить лайк
- `DELETE /api/like?postId=...` - Убрать лайк
- `POST /api/follow?userId=...` - Подписаться
- `DELETE /api/follow?userId=...` - Отписаться
- `POST /api/comments?postId=...` - Добавить комментарий

### Уведомления
- `GET /api/notifications/[userId]` - Получить уведомления пользователя

## 🚀 Скрипты

```bash
# Запустить dev сервер
npm run dev

# Собрать production build
npm run build

# Запустить production сервер
npm start

# Запустить линтер
npm run lint

# Сгенерировать Prisma Client
npx prisma generate

# Применить схему к базе данных
npx prisma db push

# Открыть Prisma Studio (GUI для БД)
npx prisma studio
```

## 🔧 Конфигурация

### TypeScript
- Strict mode включен
- Path alias `@/*` указывает на корень проекта
- Target: ES2017

### Prisma
- Custom output path: `prisma/client/`
- MongoDB provider
- Каскадное удаление для связанных записей

## 📖 Использование

1. **Регистрация**: Создайте аккаунт с помощью формы регистрации
2. **Создание постов**: Напишите твит в форме на главной странице
3. **Взаимодействие**: Лайкайте, комментируйте посты других пользователей
4. **Подписки**: Подписывайтесь на интересных пользователей
5. **Уведомления**: Отслеживайте активность в разделе уведомлений
6. **Профиль**: Настройте свой профиль, загрузите аватар и обложку

## 🤝 Вклад в проект

Стиль коммитов: сообщения на русском языке в нижнем регистре

Примеры:
```
добавление функционала комментариев
исправление бага с уведомлениями
рефакторинг компонента PostItem
```

## 📄 Лицензия

MIT

## 👤 Автор

Kirill Taratun

---

Создано с ❤️ используя Next.js и современные веб-технологии
